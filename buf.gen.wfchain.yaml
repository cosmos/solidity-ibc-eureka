# Separate generation config for wfchain protos using gogoproto
version: v2
managed:
  enabled: true

plugins:
  - local: protoc-gen-gocosmos
    out: e2e/interchaintestv8/types/
    opt: paths=source_relative

inputs:
  - directory: proto
