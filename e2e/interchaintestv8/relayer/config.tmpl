{
  "server": {
    "log_level": "{{ .LogLevel }}",
    "address": "{{ .Address }}",
    "port": {{ .Port }},
    "grpc_web_port": {{ .GrpcWebPort }}
  },
  "modules": [
    {{- range $index, $module := .Modules }}
    {{- if $index }},{{ end }}
    {{ $module.ToJSON }}
    {{- end }}
  ]
}
