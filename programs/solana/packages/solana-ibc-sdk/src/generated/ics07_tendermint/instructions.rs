// AUTO-GENERATED from ics07_tendermint IDL - do not edit
// Generated by build.rs from Anchor IDL files
//
// These structs provide typed instruction account builders that match
// the exact account ordering defined in the Anchor program.

#![allow(unused_imports)]

use anchor_lang::solana_program::instruction::{AccountMeta, Instruction};
use anchor_lang::solana_program::pubkey::Pubkey;

/// Input accounts for the `initialize` instruction.
pub struct InitializeAccounts {
    pub payer: Pubkey,
    pub revision_height: u64,
}

/// Resolved instruction accounts for `initialize`.
pub struct Initialize {
    program_id: Pubkey,
    client_state_account: Pubkey,
    consensus_state_store: Pubkey,
    app_state: Pubkey,
    payer: Pubkey,
}

impl Initialize {
    /// Total number of accounts (including fixed-address accounts).
    pub const COUNT: usize = 5;

    /// Anchor instruction discriminator.
    pub const DISCRIMINATOR: [u8; 8] = [175, 175, 109, 31, 13, 152, 155, 237];

    #[must_use]
    pub fn client_state_account_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"client"], program_id)
    }

    #[must_use]
    pub fn consensus_state_store_pda(revision_height: u64, program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(
            &[b"consensus_state", &revision_height.to_le_bytes()],
            program_id,
        )
    }

    #[must_use]
    pub fn app_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"app_state"], program_id)
    }

    /// Creates a new instruction builder, auto-deriving PDA accounts.
    #[must_use]
    pub fn new(accounts: InitializeAccounts, program_id: &Pubkey) -> Self {
        let (client_state_account, _) = Self::client_state_account_pda(program_id);
        let (consensus_state_store, _) =
            Self::consensus_state_store_pda(accounts.revision_height, program_id);
        let (app_state, _) = Self::app_state_pda(program_id);
        Self {
            program_id: *program_id,
            client_state_account,
            consensus_state_store,
            app_state,
            payer: accounts.payer,
        }
    }

    /// Returns account metas in the order expected by the program.
    #[must_use]
    pub fn to_account_metas(&self) -> Vec<AccountMeta> {
        vec![
            AccountMeta::new(self.client_state_account, false),
            AccountMeta::new(self.consensus_state_store, false),
            AccountMeta::new(self.app_state, false),
            AccountMeta::new(self.payer, true),
            AccountMeta::new_readonly(anchor_lang::solana_program::system_program::ID, false),
        ]
    }

    /// Builds a complete [`Instruction`] with discriminator, args data and remaining accounts.
    #[must_use]
    pub fn build_instruction(
        self,
        args_data: &[u8],
        remaining_accounts: impl IntoIterator<Item = AccountMeta>,
    ) -> Instruction {
        let mut accounts = self.to_account_metas();
        accounts.extend(remaining_accounts);
        let mut data = Self::DISCRIMINATOR.to_vec();
        data.extend_from_slice(args_data);
        Instruction {
            program_id: self.program_id,
            accounts,
            data,
        }
    }
}

/// Input accounts for the `set_access_manager` instruction.
pub struct SetAccessManagerAccounts {
    pub access_manager: Pubkey,
    pub admin: Pubkey,
}

/// Resolved instruction accounts for `set_access_manager`.
pub struct SetAccessManager {
    program_id: Pubkey,
    app_state: Pubkey,
    access_manager: Pubkey,
    admin: Pubkey,
}

impl SetAccessManager {
    /// Total number of accounts (including fixed-address accounts).
    pub const COUNT: usize = 4;

    /// Anchor instruction discriminator.
    pub const DISCRIMINATOR: [u8; 8] = [95, 209, 134, 89, 195, 69, 35, 122];

    #[must_use]
    pub fn app_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"app_state"], program_id)
    }

    /// Creates a new instruction builder, auto-deriving PDA accounts.
    #[must_use]
    pub fn new(accounts: SetAccessManagerAccounts, program_id: &Pubkey) -> Self {
        let (app_state, _) = Self::app_state_pda(program_id);
        Self {
            program_id: *program_id,
            app_state,
            access_manager: accounts.access_manager,
            admin: accounts.admin,
        }
    }

    /// Returns account metas in the order expected by the program.
    #[must_use]
    pub fn to_account_metas(&self) -> Vec<AccountMeta> {
        vec![
            AccountMeta::new(self.app_state, false),
            AccountMeta::new_readonly(self.access_manager, false),
            AccountMeta::new_readonly(self.admin, true),
            AccountMeta::new_readonly(anchor_lang::solana_program::sysvar::instructions::ID, false),
        ]
    }

    /// Builds a complete [`Instruction`] with discriminator, args data and remaining accounts.
    #[must_use]
    pub fn build_instruction(
        self,
        args_data: &[u8],
        remaining_accounts: impl IntoIterator<Item = AccountMeta>,
    ) -> Instruction {
        let mut accounts = self.to_account_metas();
        accounts.extend(remaining_accounts);
        let mut data = Self::DISCRIMINATOR.to_vec();
        data.extend_from_slice(args_data);
        Instruction {
            program_id: self.program_id,
            accounts,
            data,
        }
    }
}

/// Input accounts for the `verify_membership` instruction.
pub struct VerifyMembershipAccounts {
    pub height: u64,
}

/// Resolved instruction accounts for `verify_membership`.
pub struct VerifyMembership {
    program_id: Pubkey,
    client_state: Pubkey,
    consensus_state_at_height: Pubkey,
}

impl VerifyMembership {
    /// Total number of accounts (including fixed-address accounts).
    pub const COUNT: usize = 2;

    /// Anchor instruction discriminator.
    pub const DISCRIMINATOR: [u8; 8] = [101, 53, 78, 0, 103, 151, 236, 209];

    #[must_use]
    pub fn client_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"client"], program_id)
    }

    #[must_use]
    pub fn consensus_state_at_height_pda(height: u64, program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"consensus_state", &height.to_le_bytes()], program_id)
    }

    /// Creates a new instruction builder, auto-deriving PDA accounts.
    #[must_use]
    pub fn new(accounts: VerifyMembershipAccounts, program_id: &Pubkey) -> Self {
        let (client_state, _) = Self::client_state_pda(program_id);
        let (consensus_state_at_height, _) =
            Self::consensus_state_at_height_pda(accounts.height, program_id);
        Self {
            program_id: *program_id,
            client_state,
            consensus_state_at_height,
        }
    }

    /// Returns account metas in the order expected by the program.
    #[must_use]
    pub fn to_account_metas(&self) -> Vec<AccountMeta> {
        vec![
            AccountMeta::new_readonly(self.client_state, false),
            AccountMeta::new_readonly(self.consensus_state_at_height, false),
        ]
    }

    /// Builds a complete [`Instruction`] with discriminator, args data and remaining accounts.
    #[must_use]
    pub fn build_instruction(
        self,
        args_data: &[u8],
        remaining_accounts: impl IntoIterator<Item = AccountMeta>,
    ) -> Instruction {
        let mut accounts = self.to_account_metas();
        accounts.extend(remaining_accounts);
        let mut data = Self::DISCRIMINATOR.to_vec();
        data.extend_from_slice(args_data);
        Instruction {
            program_id: self.program_id,
            accounts,
            data,
        }
    }
}

/// Input accounts for the `verify_non_membership` instruction.
pub struct VerifyNonMembershipAccounts {
    pub height: u64,
}

/// Resolved instruction accounts for `verify_non_membership`.
pub struct VerifyNonMembership {
    program_id: Pubkey,
    client_state: Pubkey,
    consensus_state_at_height: Pubkey,
}

impl VerifyNonMembership {
    /// Total number of accounts (including fixed-address accounts).
    pub const COUNT: usize = 2;

    /// Anchor instruction discriminator.
    pub const DISCRIMINATOR: [u8; 8] = [231, 161, 86, 239, 111, 236, 14, 74];

    #[must_use]
    pub fn client_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"client"], program_id)
    }

    #[must_use]
    pub fn consensus_state_at_height_pda(height: u64, program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"consensus_state", &height.to_le_bytes()], program_id)
    }

    /// Creates a new instruction builder, auto-deriving PDA accounts.
    #[must_use]
    pub fn new(accounts: VerifyNonMembershipAccounts, program_id: &Pubkey) -> Self {
        let (client_state, _) = Self::client_state_pda(program_id);
        let (consensus_state_at_height, _) =
            Self::consensus_state_at_height_pda(accounts.height, program_id);
        Self {
            program_id: *program_id,
            client_state,
            consensus_state_at_height,
        }
    }

    /// Returns account metas in the order expected by the program.
    #[must_use]
    pub fn to_account_metas(&self) -> Vec<AccountMeta> {
        vec![
            AccountMeta::new_readonly(self.client_state, false),
            AccountMeta::new_readonly(self.consensus_state_at_height, false),
        ]
    }

    /// Builds a complete [`Instruction`] with discriminator, args data and remaining accounts.
    #[must_use]
    pub fn build_instruction(
        self,
        args_data: &[u8],
        remaining_accounts: impl IntoIterator<Item = AccountMeta>,
    ) -> Instruction {
        let mut accounts = self.to_account_metas();
        accounts.extend(remaining_accounts);
        let mut data = Self::DISCRIMINATOR.to_vec();
        data.extend_from_slice(args_data);
        Instruction {
            program_id: self.program_id,
            accounts,
            data,
        }
    }
}

/// Input accounts for the `upload_header_chunk` instruction.
pub struct UploadHeaderChunkAccounts {
    pub chunk: Pubkey,
    pub access_manager: Pubkey,
    pub submitter: Pubkey,
}

/// Resolved instruction accounts for `upload_header_chunk`.
pub struct UploadHeaderChunk {
    program_id: Pubkey,
    chunk: Pubkey,
    client_state: Pubkey,
    app_state: Pubkey,
    access_manager: Pubkey,
    submitter: Pubkey,
}

impl UploadHeaderChunk {
    /// Total number of accounts (including fixed-address accounts).
    pub const COUNT: usize = 7;

    /// Anchor instruction discriminator.
    pub const DISCRIMINATOR: [u8; 8] = [154, 38, 82, 143, 56, 2, 24, 33];

    #[must_use]
    pub fn client_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"client"], program_id)
    }

    #[must_use]
    pub fn app_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"app_state"], program_id)
    }

    /// Creates a new instruction builder, auto-deriving PDA accounts.
    #[must_use]
    pub fn new(accounts: UploadHeaderChunkAccounts, program_id: &Pubkey) -> Self {
        let (client_state, _) = Self::client_state_pda(program_id);
        let (app_state, _) = Self::app_state_pda(program_id);
        Self {
            program_id: *program_id,
            chunk: accounts.chunk,
            client_state,
            app_state,
            access_manager: accounts.access_manager,
            submitter: accounts.submitter,
        }
    }

    /// Returns account metas in the order expected by the program.
    #[must_use]
    pub fn to_account_metas(&self) -> Vec<AccountMeta> {
        vec![
            AccountMeta::new(self.chunk, false),
            AccountMeta::new_readonly(self.client_state, false),
            AccountMeta::new_readonly(self.app_state, false),
            AccountMeta::new_readonly(self.access_manager, false),
            AccountMeta::new(self.submitter, true),
            AccountMeta::new_readonly(anchor_lang::solana_program::sysvar::instructions::ID, false),
            AccountMeta::new_readonly(anchor_lang::solana_program::system_program::ID, false),
        ]
    }

    /// Builds a complete [`Instruction`] with discriminator, args data and remaining accounts.
    #[must_use]
    pub fn build_instruction(
        self,
        args_data: &[u8],
        remaining_accounts: impl IntoIterator<Item = AccountMeta>,
    ) -> Instruction {
        let mut accounts = self.to_account_metas();
        accounts.extend(remaining_accounts);
        let mut data = Self::DISCRIMINATOR.to_vec();
        data.extend_from_slice(args_data);
        Instruction {
            program_id: self.program_id,
            accounts,
            data,
        }
    }
}

/// Input accounts for the `assemble_and_update_client` instruction.
pub struct AssembleAndUpdateClientAccounts {
    pub access_manager: Pubkey,
    pub submitter: Pubkey,
    pub trusted_height: u64,
    pub target_height: u64,
}

/// Resolved instruction accounts for `assemble_and_update_client`.
pub struct AssembleAndUpdateClient {
    program_id: Pubkey,
    client_state: Pubkey,
    app_state: Pubkey,
    access_manager: Pubkey,
    trusted_consensus_state: Pubkey,
    new_consensus_state_store: Pubkey,
    submitter: Pubkey,
}

impl AssembleAndUpdateClient {
    /// Total number of accounts (including fixed-address accounts).
    pub const COUNT: usize = 8;

    /// Anchor instruction discriminator.
    pub const DISCRIMINATOR: [u8; 8] = [86, 215, 199, 79, 131, 79, 180, 158];

    #[must_use]
    pub fn client_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"client"], program_id)
    }

    #[must_use]
    pub fn app_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"app_state"], program_id)
    }

    #[must_use]
    pub fn trusted_consensus_state_pda(trusted_height: u64, program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(
            &[b"consensus_state", &trusted_height.to_le_bytes()],
            program_id,
        )
    }

    #[must_use]
    pub fn new_consensus_state_store_pda(target_height: u64, program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(
            &[b"consensus_state", &target_height.to_le_bytes()],
            program_id,
        )
    }

    /// Creates a new instruction builder, auto-deriving PDA accounts.
    #[must_use]
    pub fn new(accounts: AssembleAndUpdateClientAccounts, program_id: &Pubkey) -> Self {
        let (client_state, _) = Self::client_state_pda(program_id);
        let (app_state, _) = Self::app_state_pda(program_id);
        let (trusted_consensus_state, _) =
            Self::trusted_consensus_state_pda(accounts.trusted_height, program_id);
        let (new_consensus_state_store, _) =
            Self::new_consensus_state_store_pda(accounts.target_height, program_id);
        Self {
            program_id: *program_id,
            client_state,
            app_state,
            access_manager: accounts.access_manager,
            trusted_consensus_state,
            new_consensus_state_store,
            submitter: accounts.submitter,
        }
    }

    /// Returns account metas in the order expected by the program.
    #[must_use]
    pub fn to_account_metas(&self) -> Vec<AccountMeta> {
        vec![
            AccountMeta::new(self.client_state, false),
            AccountMeta::new_readonly(self.app_state, false),
            AccountMeta::new_readonly(self.access_manager, false),
            AccountMeta::new_readonly(self.trusted_consensus_state, false),
            AccountMeta::new(self.new_consensus_state_store, false),
            AccountMeta::new(self.submitter, true),
            AccountMeta::new_readonly(anchor_lang::solana_program::system_program::ID, false),
            AccountMeta::new_readonly(anchor_lang::solana_program::sysvar::instructions::ID, false),
        ]
    }

    /// Builds a complete [`Instruction`] with discriminator, args data and remaining accounts.
    #[must_use]
    pub fn build_instruction(
        self,
        args_data: &[u8],
        remaining_accounts: impl IntoIterator<Item = AccountMeta>,
    ) -> Instruction {
        let mut accounts = self.to_account_metas();
        accounts.extend(remaining_accounts);
        let mut data = Self::DISCRIMINATOR.to_vec();
        data.extend_from_slice(args_data);
        Instruction {
            program_id: self.program_id,
            accounts,
            data,
        }
    }
}

/// Input accounts for the `cleanup_incomplete_upload` instruction.
pub struct CleanupIncompleteUploadAccounts {
    pub submitter: Pubkey,
}

/// Resolved instruction accounts for `cleanup_incomplete_upload`.
pub struct CleanupIncompleteUpload {
    program_id: Pubkey,
    submitter: Pubkey,
}

impl CleanupIncompleteUpload {
    /// Total number of accounts (including fixed-address accounts).
    pub const COUNT: usize = 1;

    /// Anchor instruction discriminator.
    pub const DISCRIMINATOR: [u8; 8] = [53, 54, 142, 122, 91, 50, 60, 171];
    /// Creates a new instruction builder, auto-deriving PDA accounts.
    #[must_use]
    pub fn new(accounts: CleanupIncompleteUploadAccounts, program_id: &Pubkey) -> Self {
        Self {
            program_id: *program_id,
            submitter: accounts.submitter,
        }
    }

    /// Returns account metas in the order expected by the program.
    #[must_use]
    pub fn to_account_metas(&self) -> Vec<AccountMeta> {
        vec![AccountMeta::new(self.submitter, true)]
    }

    /// Builds a complete [`Instruction`] with discriminator, args data and remaining accounts.
    #[must_use]
    pub fn build_instruction(
        self,
        args_data: &[u8],
        remaining_accounts: impl IntoIterator<Item = AccountMeta>,
    ) -> Instruction {
        let mut accounts = self.to_account_metas();
        accounts.extend(remaining_accounts);
        let mut data = Self::DISCRIMINATOR.to_vec();
        data.extend_from_slice(args_data);
        Instruction {
            program_id: self.program_id,
            accounts,
            data,
        }
    }
}

/// Input accounts for the `upload_misbehaviour_chunk` instruction.
pub struct UploadMisbehaviourChunkAccounts {
    pub chunk: Pubkey,
    pub access_manager: Pubkey,
    pub submitter: Pubkey,
}

/// Resolved instruction accounts for `upload_misbehaviour_chunk`.
pub struct UploadMisbehaviourChunk {
    program_id: Pubkey,
    chunk: Pubkey,
    client_state: Pubkey,
    app_state: Pubkey,
    access_manager: Pubkey,
    submitter: Pubkey,
}

impl UploadMisbehaviourChunk {
    /// Total number of accounts (including fixed-address accounts).
    pub const COUNT: usize = 7;

    /// Anchor instruction discriminator.
    pub const DISCRIMINATOR: [u8; 8] = [150, 89, 208, 203, 92, 179, 160, 140];

    #[must_use]
    pub fn client_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"client"], program_id)
    }

    #[must_use]
    pub fn app_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"app_state"], program_id)
    }

    /// Creates a new instruction builder, auto-deriving PDA accounts.
    #[must_use]
    pub fn new(accounts: UploadMisbehaviourChunkAccounts, program_id: &Pubkey) -> Self {
        let (client_state, _) = Self::client_state_pda(program_id);
        let (app_state, _) = Self::app_state_pda(program_id);
        Self {
            program_id: *program_id,
            chunk: accounts.chunk,
            client_state,
            app_state,
            access_manager: accounts.access_manager,
            submitter: accounts.submitter,
        }
    }

    /// Returns account metas in the order expected by the program.
    #[must_use]
    pub fn to_account_metas(&self) -> Vec<AccountMeta> {
        vec![
            AccountMeta::new(self.chunk, false),
            AccountMeta::new_readonly(self.client_state, false),
            AccountMeta::new_readonly(self.app_state, false),
            AccountMeta::new_readonly(self.access_manager, false),
            AccountMeta::new(self.submitter, true),
            AccountMeta::new_readonly(anchor_lang::solana_program::sysvar::instructions::ID, false),
            AccountMeta::new_readonly(anchor_lang::solana_program::system_program::ID, false),
        ]
    }

    /// Builds a complete [`Instruction`] with discriminator, args data and remaining accounts.
    #[must_use]
    pub fn build_instruction(
        self,
        args_data: &[u8],
        remaining_accounts: impl IntoIterator<Item = AccountMeta>,
    ) -> Instruction {
        let mut accounts = self.to_account_metas();
        accounts.extend(remaining_accounts);
        let mut data = Self::DISCRIMINATOR.to_vec();
        data.extend_from_slice(args_data);
        Instruction {
            program_id: self.program_id,
            accounts,
            data,
        }
    }
}

/// Input accounts for the `assemble_and_submit_misbehaviour` instruction.
pub struct AssembleAndSubmitMisbehaviourAccounts {
    pub access_manager: Pubkey,
    pub submitter: Pubkey,
    pub trusted_height_1: u64,
    pub trusted_height_2: u64,
}

/// Resolved instruction accounts for `assemble_and_submit_misbehaviour`.
pub struct AssembleAndSubmitMisbehaviour {
    program_id: Pubkey,
    client_state: Pubkey,
    app_state: Pubkey,
    access_manager: Pubkey,
    trusted_consensus_state_1: Pubkey,
    trusted_consensus_state_2: Pubkey,
    submitter: Pubkey,
}

impl AssembleAndSubmitMisbehaviour {
    /// Total number of accounts (including fixed-address accounts).
    pub const COUNT: usize = 7;

    /// Anchor instruction discriminator.
    pub const DISCRIMINATOR: [u8; 8] = [182, 151, 225, 14, 100, 151, 177, 234];

    #[must_use]
    pub fn client_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"client"], program_id)
    }

    #[must_use]
    pub fn app_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"app_state"], program_id)
    }

    #[must_use]
    pub fn trusted_consensus_state_1_pda(
        trusted_height_1: u64,
        program_id: &Pubkey,
    ) -> (Pubkey, u8) {
        Pubkey::find_program_address(
            &[b"consensus_state", &trusted_height_1.to_le_bytes()],
            program_id,
        )
    }

    #[must_use]
    pub fn trusted_consensus_state_2_pda(
        trusted_height_2: u64,
        program_id: &Pubkey,
    ) -> (Pubkey, u8) {
        Pubkey::find_program_address(
            &[b"consensus_state", &trusted_height_2.to_le_bytes()],
            program_id,
        )
    }

    /// Creates a new instruction builder, auto-deriving PDA accounts.
    #[must_use]
    pub fn new(accounts: AssembleAndSubmitMisbehaviourAccounts, program_id: &Pubkey) -> Self {
        let (client_state, _) = Self::client_state_pda(program_id);
        let (app_state, _) = Self::app_state_pda(program_id);
        let (trusted_consensus_state_1, _) =
            Self::trusted_consensus_state_1_pda(accounts.trusted_height_1, program_id);
        let (trusted_consensus_state_2, _) =
            Self::trusted_consensus_state_2_pda(accounts.trusted_height_2, program_id);
        Self {
            program_id: *program_id,
            client_state,
            app_state,
            access_manager: accounts.access_manager,
            trusted_consensus_state_1,
            trusted_consensus_state_2,
            submitter: accounts.submitter,
        }
    }

    /// Returns account metas in the order expected by the program.
    #[must_use]
    pub fn to_account_metas(&self) -> Vec<AccountMeta> {
        vec![
            AccountMeta::new(self.client_state, false),
            AccountMeta::new_readonly(self.app_state, false),
            AccountMeta::new_readonly(self.access_manager, false),
            AccountMeta::new_readonly(self.trusted_consensus_state_1, false),
            AccountMeta::new_readonly(self.trusted_consensus_state_2, false),
            AccountMeta::new(self.submitter, true),
            AccountMeta::new_readonly(anchor_lang::solana_program::sysvar::instructions::ID, false),
        ]
    }

    /// Builds a complete [`Instruction`] with discriminator, args data and remaining accounts.
    #[must_use]
    pub fn build_instruction(
        self,
        args_data: &[u8],
        remaining_accounts: impl IntoIterator<Item = AccountMeta>,
    ) -> Instruction {
        let mut accounts = self.to_account_metas();
        accounts.extend(remaining_accounts);
        let mut data = Self::DISCRIMINATOR.to_vec();
        data.extend_from_slice(args_data);
        Instruction {
            program_id: self.program_id,
            accounts,
            data,
        }
    }
}

/// Input accounts for the `cleanup_incomplete_misbehaviour` instruction.
pub struct CleanupIncompleteMisbehaviourAccounts {
    pub submitter: Pubkey,
}

/// Resolved instruction accounts for `cleanup_incomplete_misbehaviour`.
pub struct CleanupIncompleteMisbehaviour {
    program_id: Pubkey,
    submitter: Pubkey,
}

impl CleanupIncompleteMisbehaviour {
    /// Total number of accounts (including fixed-address accounts).
    pub const COUNT: usize = 1;

    /// Anchor instruction discriminator.
    pub const DISCRIMINATOR: [u8; 8] = [128, 250, 150, 138, 153, 111, 26, 224];
    /// Creates a new instruction builder, auto-deriving PDA accounts.
    #[must_use]
    pub fn new(accounts: CleanupIncompleteMisbehaviourAccounts, program_id: &Pubkey) -> Self {
        Self {
            program_id: *program_id,
            submitter: accounts.submitter,
        }
    }

    /// Returns account metas in the order expected by the program.
    #[must_use]
    pub fn to_account_metas(&self) -> Vec<AccountMeta> {
        vec![AccountMeta::new(self.submitter, true)]
    }

    /// Builds a complete [`Instruction`] with discriminator, args data and remaining accounts.
    #[must_use]
    pub fn build_instruction(
        self,
        args_data: &[u8],
        remaining_accounts: impl IntoIterator<Item = AccountMeta>,
    ) -> Instruction {
        let mut accounts = self.to_account_metas();
        accounts.extend(remaining_accounts);
        let mut data = Self::DISCRIMINATOR.to_vec();
        data.extend_from_slice(args_data);
        Instruction {
            program_id: self.program_id,
            accounts,
            data,
        }
    }
}

/// Input accounts for the `pre_verify_signature` instruction.
pub struct PreVerifySignatureAccounts {
    pub signature_verification: Pubkey,
    pub access_manager: Pubkey,
    pub submitter: Pubkey,
}

/// Resolved instruction accounts for `pre_verify_signature`.
pub struct PreVerifySignature {
    program_id: Pubkey,
    signature_verification: Pubkey,
    app_state: Pubkey,
    access_manager: Pubkey,
    submitter: Pubkey,
}

impl PreVerifySignature {
    /// Total number of accounts (including fixed-address accounts).
    pub const COUNT: usize = 6;

    /// Anchor instruction discriminator.
    pub const DISCRIMINATOR: [u8; 8] = [121, 29, 68, 190, 85, 124, 111, 45];

    #[must_use]
    pub fn app_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"app_state"], program_id)
    }

    /// Creates a new instruction builder, auto-deriving PDA accounts.
    #[must_use]
    pub fn new(accounts: PreVerifySignatureAccounts, program_id: &Pubkey) -> Self {
        let (app_state, _) = Self::app_state_pda(program_id);
        Self {
            program_id: *program_id,
            signature_verification: accounts.signature_verification,
            app_state,
            access_manager: accounts.access_manager,
            submitter: accounts.submitter,
        }
    }

    /// Returns account metas in the order expected by the program.
    #[must_use]
    pub fn to_account_metas(&self) -> Vec<AccountMeta> {
        vec![
            AccountMeta::new_readonly(anchor_lang::solana_program::sysvar::instructions::ID, false),
            AccountMeta::new(self.signature_verification, false),
            AccountMeta::new_readonly(self.app_state, false),
            AccountMeta::new_readonly(self.access_manager, false),
            AccountMeta::new(self.submitter, true),
            AccountMeta::new_readonly(anchor_lang::solana_program::system_program::ID, false),
        ]
    }

    /// Builds a complete [`Instruction`] with discriminator, args data and remaining accounts.
    #[must_use]
    pub fn build_instruction(
        self,
        args_data: &[u8],
        remaining_accounts: impl IntoIterator<Item = AccountMeta>,
    ) -> Instruction {
        let mut accounts = self.to_account_metas();
        accounts.extend(remaining_accounts);
        let mut data = Self::DISCRIMINATOR.to_vec();
        data.extend_from_slice(args_data);
        Instruction {
            program_id: self.program_id,
            accounts,
            data,
        }
    }
}

/// Input accounts for the `client_status` instruction.
pub struct ClientStatusAccounts {
    pub consensus_state: Pubkey,
}

/// Resolved instruction accounts for `client_status`.
pub struct ClientStatus {
    program_id: Pubkey,
    client_state: Pubkey,
    consensus_state: Pubkey,
}

impl ClientStatus {
    /// Total number of accounts (including fixed-address accounts).
    pub const COUNT: usize = 2;

    /// Anchor instruction discriminator.
    pub const DISCRIMINATOR: [u8; 8] = [75, 93, 126, 21, 10, 112, 109, 139];

    #[must_use]
    pub fn client_state_pda(program_id: &Pubkey) -> (Pubkey, u8) {
        Pubkey::find_program_address(&[b"client"], program_id)
    }

    /// Creates a new instruction builder, auto-deriving PDA accounts.
    #[must_use]
    pub fn new(accounts: ClientStatusAccounts, program_id: &Pubkey) -> Self {
        let (client_state, _) = Self::client_state_pda(program_id);
        Self {
            program_id: *program_id,
            client_state,
            consensus_state: accounts.consensus_state,
        }
    }

    /// Returns account metas in the order expected by the program.
    #[must_use]
    pub fn to_account_metas(&self) -> Vec<AccountMeta> {
        vec![
            AccountMeta::new_readonly(self.client_state, false),
            AccountMeta::new_readonly(self.consensus_state, false),
        ]
    }

    /// Builds a complete [`Instruction`] with discriminator, args data and remaining accounts.
    #[must_use]
    pub fn build_instruction(
        self,
        args_data: &[u8],
        remaining_accounts: impl IntoIterator<Item = AccountMeta>,
    ) -> Instruction {
        let mut accounts = self.to_account_metas();
        accounts.extend(remaining_accounts);
        let mut data = Self::DISCRIMINATOR.to_vec();
        data.extend_from_slice(args_data);
        Instruction {
            program_id: self.program_id,
            accounts,
            data,
        }
    }
}
