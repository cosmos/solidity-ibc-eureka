[package]
name        = "tendermint-light-client-membership"
description = "Verify (non)membership program for tendermint-light-client"
version     = { workspace = true }
edition     = { workspace = true }
repository  = { workspace = true }
license     = { workspace = true }

[features]
default = ["ethereum"]
ethereum = ["dep:ibc-eureka-solidity-types"]
solana = []

[dependencies]
# Ethereum-specific dependencies
ibc-eureka-solidity-types     = { workspace = true, optional = true }

# Shared dependencies
ibc-core-commitment-types     = { workspace = true }
